# 📖 Multi-Agent 系统操作手册

**Complete Command Reference - 完整指令参考**

---

## 🎯 快速导航

- [日常记录操作](#日常记录操作) - Learning、Best Practice、Reflection
- [Agent 操作](#agent-操作) - 创建、修改、查看 Agents
- [Skill 操作](#skill-操作) - 创建、修改、查看 Skills
- [Git 同步操作](#git-同步操作) - 提交、推送、拉取
- [查找和搜索](#查找和搜索) - 快速定位文件和内容
- [快捷命令集](#快捷命令集) - 常用命令组合
- [故障排查](#故障排查) - 常见问题解决

---

## 📝 日常记录操作

### 1. 创建 Learning Entry（学习记录）

**场景：** 每次完成重要任务、解决问题、有新发现时

```bash
# 进入系统目录
cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 方式 1: 使用模板创建
cp knowledge/_templates/learning-template.md \
   knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-你的主题.md

# 方式 2: 直接编辑（会自动创建文件）
# 确保目录存在
mkdir -p knowledge/learnings/$(date +%Y-%m)

# 用编辑器打开新文件
open knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-ppc-optimization-insight.md
```

**快速创建脚本：**
```bash
# 创建别名（添加到 ~/.zshrc 或 ~/.bashrc）
alias new-learning='cp knowledge/_templates/learning-template.md knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-temp.md && open knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-temp.md'

# 使用
new-learning
```

**填写核心内容：**
```markdown
---
learning_id: 2025-11-05-ppc-data-quality
date: 2025-11-05
category: data-quality
tags: [ppc, validation]
severity: high
agent: ad-analyzer
skill: data-analysis/data-validation
---

# 数据质量验证的重要性

## 🎯 问题/场景
[描述你遇到的问题]

## 🔍 分析和发现
[你的发现和洞察]

## ✅ 采取的行动
[你是如何解决的]

## 📊 结果
[结果如何]

## ⭐ 可提取的最佳实践
[可以复用的经验]
```

---

### 2. 创建 Best Practice（最佳实践）

**场景：** 当同类 Learning 积累到 3+ 条，或形成系统性方法时

```bash
# 创建 Best Practice
cp knowledge/_templates/best-practice-template.md \
   knowledge/best-practices/advertising/campaign-optimization-checklist.md

# 直接编辑
open knowledge/best-practices/advertising/campaign-optimization-checklist.md
```

**命名规范：**
- 文件名：小写，连字符分隔
- 例如：`data-quality-checklist.md`, `keyword-research-workflow.md`

---

### 3. 创建 Reflection（反思记录）

**场景：** 每周/月定期反思，或遇到重大事件时

```bash
# 创建本月 Reflection
mkdir -p knowledge/reflections/$(date +%Y-%m)

cp knowledge/_templates/reflection-template.md \
   knowledge/reflections/$(date +%Y-%m)/$(date +%Y-%m-%d)-weekly-reflection.md

# 编辑
open knowledge/reflections/$(date +%Y-%m)/$(date +%Y-%m-%d)-weekly-reflection.md
```

---

### 4. 查看和编辑已有记录

```bash
# 查看本月所有 Learnings
ls -lt knowledge/learnings/$(date +%Y-%m)/

# 编辑最近的 Learning
open knowledge/learnings/$(date +%Y-%m)/*.md | tail -1

# 查看所有 Best Practices
find knowledge/best-practices/ -name "*.md" -type f

# 搜索包含特定关键词的 Learning
grep -r "PPC" knowledge/learnings/
```

---

## 🤖 Agent 操作

### 1. 创建新 Agent

```bash
# 1. 创建 Agent 目录
mkdir -p agents/你的agent名称/{workflows,examples}

# 2. 复制模板
cp agents/_templates/agent-template.md agents/你的agent名称/agent.md

# 3. 编辑 Agent 定义
open agents/你的agent名称/agent.md
```

**示例 - 创建 Keyword Researcher Agent：**
```bash
# 创建目录
mkdir -p agents/keyword-researcher/{workflows,examples}

# 复制模板
cp agents/_templates/agent-template.md agents/keyword-researcher/agent.md

# 编辑
open agents/keyword-researcher/agent.md
```

**填写关键信息：**
```yaml
---
name: keyword-researcher
description: |
  专门用于亚马逊关键词研究和分析的 Agent
model: opus
color: green
version: 1.0.0
skills:
  - amazon/seo/keyword-research
  - data-analysis/trend-detection
---
```

---

### 2. 查看和修改 Agent

```bash
# 列出所有 Agents
ls -d agents/*/

# 查看特定 Agent
cat agents/ad-analyzer/agent.md

# 编辑 Agent
open agents/ad-analyzer/agent.md

# 查看 Agent 的工作流
ls agents/ad-analyzer/workflows/

# 查看 Agent 的示例
ls agents/ad-analyzer/examples/
```

---

### 3. 测试 Agent（与 Claude Code 集成）

```bash
# 方式 1: 创建软链接
ln -s "$(pwd)/agents/ad-analyzer" ~/.claude/agents/ad-analyzer

# 方式 2: 直接复制
cp -r agents/ad-analyzer ~/.claude/agents/

# 验证
ls ~/.claude/agents/
```

**在 Claude Code 中使用：**
```
@ad-analyzer 分析我的 PPC 数据
```

---

## 📚 Skill 操作

### 1. 创建新 Skill

```bash
# 1. 确定 Skill 分类和位置
# 分类: amazon/advertising, amazon/seo, data-analysis, reporting

# 2. 创建 Skill 文件
cp skills/_templates/skill-template.md \
   skills/amazon/seo/keyword-research.md

# 3. 编辑 Skill
open skills/amazon/seo/keyword-research.md
```

**填写 Frontmatter：**
```yaml
---
skill_id: amazon-seo-keyword-research
skill_name: 关键词研究技能
category: amazon/seo
version: 1.0.0
created: 2025-11-05
updated: 2025-11-05
status: active

dependencies:
  - data-analysis-trend-detection

tags:
  - keyword
  - seo
  - research
---
```

---

### 2. 修改现有 Skill

```bash
# 查看 Skill
cat skills/amazon/advertising/ppc-optimization.md

# 编辑 Skill
open skills/amazon/advertising/ppc-optimization.md

# 更新版本号（在 frontmatter 中）
# version: 1.0.0 -> 1.1.0
```

**更新 Skill 后的操作：**
```bash
# 1. 更新 CHANGELOG
echo "### v1.1.0 ($(date +%Y-%m-%d))
- 优化了数据验证流程
- 新增了异常检测功能" >> skills/amazon/advertising/CHANGELOG.md

# 2. 记录 Learning
# 说明为什么更新、更新了什么

# 3. 提交到 Git
git add skills/amazon/advertising/ppc-optimization.md
git commit -m "feat: 优化 PPC optimization skill v1.1.0"
```

---

### 3. 查找和搜索 Skills

```bash
# 列出所有 Skills
find skills/ -name "*.md" -not -path "*/\.*" -not -name "README.md"

# 按类别查看
ls skills/amazon/advertising/
ls skills/amazon/seo/
ls skills/data-analysis/

# 搜索包含特定关键词的 Skill
grep -r "数据验证" skills/

# 查看 Skill 的标签
grep "^tags:" skills/**/*.md
```

---

## 🔄 Git 同步操作

### 1. 初始化仓库（首次）

```bash
cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 初始化 Git
git init

# 添加所有文件
git add .

# 首次提交
git commit -m "🎉 Initial commit: Multi-Agent 运营系统 v1.0.0

- 完整的目录结构
- Agent、Skill、Knowledge 模板
- 示例 PPC 优化 Skill
- 完整文档系统"

# 创建 GitHub 仓库后，关联远程仓库
git remote add origin https://github.com/YOUR_USERNAME/amazon-operations-multi-agent.git

# 推送
git branch -M main  # 或 master
git push -u origin main
```

---

### 2. 日常提交流程

**简单提交（单个更改）：**
```bash
# 1. 查看状态
git status

# 2. 添加文件
git add knowledge/learnings/2025-11/2025-11-05-my-learning.md

# 3. 提交
git commit -m "📝 Add PPC data quality learning"

# 4. 推送
git push
```

**批量提交（多个更改）：**
```bash
# 添加所有 Learnings
git add knowledge/learnings/

# 提交
git commit -m "📝 Add weekly learnings (Nov 5-11)

- PPC data quality validation
- Keyword optimization insights
- Campaign structure improvements"

# 推送
git push
```

---

### 3. 每日/每周同步

**每日同步脚本：**
```bash
#!/bin/bash
# 保存为 daily-sync.sh

cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 拉取最新更改
git pull

# 添加今天的更改
git add knowledge/learnings/$(date +%Y-%m)/

# 提交
git commit -m "📝 Daily sync - $(date +%Y-%m-%d)"

# 推送
git push

echo "✅ 同步完成！"
```

**使用：**
```bash
# 给脚本执行权限
chmod +x daily-sync.sh

# 运行
./daily-sync.sh
```

**每周完整同步：**
```bash
#!/bin/bash
# 保存为 weekly-sync.sh

cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 拉取
git pull

# 添加所有更改
git add .

# 查看状态
git status

# 提交
git commit -m "🔄 Weekly sync - Week of $(date +%Y-%m-%d)

Updated:
- $(git diff --cached --name-only | wc -l) files
- Learnings, Skills, and Agents updates"

# 推送
git push

echo "✅ 每周同步完成！"
```

---

### 4. 查看提交历史

```bash
# 查看最近 10 条提交
git log --oneline -10

# 查看某个文件的修改历史
git log --follow knowledge/learnings/2025-11/2025-11-05-ppc-quality.md

# 查看今天的提交
git log --since="today" --oneline

# 查看本周的提交
git log --since="1 week ago" --oneline
```

---

### 5. 撤销和恢复

```bash
# 撤销未提交的更改
git checkout -- filename.md

# 撤销 git add（取消暂存）
git reset HEAD filename.md

# 查看上一个版本的文件
git show HEAD~1:knowledge/learnings/2025-11/2025-11-05-file.md

# 恢复误删的文件
git checkout HEAD -- deleted_file.md
```

---

## 🔍 查找和搜索

### 1. 按文件名查找

```bash
# 查找所有 markdown 文件
find . -name "*.md" -type f

# 查找特定名称的文件
find . -name "*ppc*" -type f

# 查找今天创建/修改的文件
find . -name "*.md" -type f -mtime 0

# 查找最近 7 天修改的文件
find . -name "*.md" -type f -mtime -7
```

---

### 2. 按内容搜索

```bash
# 搜索包含特定关键词的文件
grep -r "PPC 优化" .

# 搜索并显示文件名和行号
grep -rn "数据验证" knowledge/

# 搜索多个关键词（AND）
grep -r "PPC" . | grep "优化"

# 搜索多个关键词（OR）
grep -rE "PPC|广告|关键词" knowledge/

# 忽略大小写搜索
grep -ri "ppc" .

# 只显示文件名（不显示内容）
grep -rl "PPC" knowledge/
```

---

### 3. 按日期查找

```bash
# 查看本月的 Learnings
ls -lt knowledge/learnings/$(date +%Y-%m)/

# 查看所有 2025 年 11 月的记录
find knowledge/learnings/2025-11/ -name "*.md"

# 查看最近修改的 5 个文件
find . -name "*.md" -type f -print0 | xargs -0 ls -lt | head -5
```

---

### 4. 按标签查找

```bash
# 查找所有包含特定标签的文件
grep -r "tags:.*ppc" knowledge/learnings/

# 查找 high severity 的 Learnings
grep -r "severity: high" knowledge/learnings/

# 查找特定 Agent 相关的记录
grep -r "agent: ad-analyzer" knowledge/learnings/
```

---

## ⚡ 快捷命令集

### 创建快捷命令别名

**添加到 `~/.zshrc` 或 `~/.bashrc`：**

```bash
# Multi-Agent 系统快捷命令

# 1. 快速进入目录
alias cdma='cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"'

# 2. 创建新 Learning
alias new-learning='cdma && mkdir -p knowledge/learnings/$(date +%Y-%m) && cp knowledge/_templates/learning-template.md knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-temp.md && open knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-temp.md'

# 3. 创建新 Best Practice
alias new-bp='cdma && cp knowledge/_templates/best-practice-template.md knowledge/best-practices/temp-bp.md && open knowledge/best-practices/temp-bp.md'

# 4. 查看今天的 Learnings
alias today-learnings='cdma && ls -la knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)*'

# 5. 查看本月 Learnings
alias month-learnings='cdma && ls -la knowledge/learnings/$(date +%Y-%m)/'

# 6. 快速 Git 同步
alias ma-sync='cdma && git add . && git commit -m "📝 Quick sync - $(date +%Y-%m-%d)" && git push'

# 7. 查看 Git 状态
alias ma-status='cdma && git status'

# 8. 搜索 Learnings
ma-search() {
    cdma && grep -rn "$1" knowledge/learnings/
}

# 9. 列出所有 Skills
alias list-skills='cdma && find skills/ -name "*.md" -not -path "*/_templates/*" -not -name "README.md"'

# 10. 列出所有 Agents
alias list-agents='cdma && ls -d agents/*/ | grep -v "_templates"'
```

**使用这些快捷命令：**
```bash
# 重新加载配置
source ~/.zshrc  # 或 source ~/.bashrc

# 快速进入目录
cdma

# 创建新 Learning
new-learning

# 查看今天的 Learnings
today-learnings

# 搜索关键词
ma-search "PPC优化"

# 快速同步
ma-sync
```

---

## 🔧 故障排查

### 问题 1: Git 推送失败

```bash
# 错误: Updates were rejected

# 解决: 先拉取远程更改
git pull --rebase origin main

# 如果有冲突，解决后：
git add .
git rebase --continue

# 然后推送
git push
```

---

### 问题 2: 找不到文件

```bash
# 检查当前目录
pwd

# 应该在系统根目录
cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 验证目录结构
ls -la

# 应该看到: agents/, skills/, knowledge/ 等目录
```

---

### 问题 3: 模板文件不存在

```bash
# 检查模板是否存在
ls knowledge/_templates/

# 如果不存在，重新创建（参考操作手册）

# 或从 Git 恢复
git checkout knowledge/_templates/
```

---

### 问题 4: 权限问题

```bash
# 给脚本添加执行权限
chmod +x daily-sync.sh

# 检查文件权限
ls -la daily-sync.sh
```

---

## 📱 快速参考卡

### 日常最常用命令 (Top 10)

```bash
# 1. 进入目录
cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"

# 2. 创建 Learning
cp knowledge/_templates/learning-template.md knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)-topic.md

# 3. 查看今天的工作
ls knowledge/learnings/$(date +%Y-%m)/$(date +%Y-%m-%d)*

# 4. 搜索内容
grep -r "关键词" knowledge/

# 5. 查看 Git 状态
git status

# 6. 添加更改
git add knowledge/learnings/

# 7. 提交
git commit -m "📝 Add learning"

# 8. 推送
git push

# 9. 查看历史
git log --oneline -5

# 10. 拉取更新
git pull
```

---

## 🎓 进阶技巧

### 1. 自动化每日 Learning 创建

**创建脚本 `create-daily-learning.sh`：**
```bash
#!/bin/bash

SYSTEM_DIR="/Users/charliephil/Desktop/广告监测 agent/运营 multi agent"
DATE=$(date +%Y-%m-%d)
MONTH=$(date +%Y-%m)
TOPIC=$1

if [ -z "$TOPIC" ]; then
    echo "用法: ./create-daily-learning.sh 你的主题"
    exit 1
fi

cd "$SYSTEM_DIR"
mkdir -p "knowledge/learnings/$MONTH"

FILE="knowledge/learnings/$MONTH/${DATE}-${TOPIC}.md"

cp "knowledge/_templates/learning-template.md" "$FILE"

# 替换日期
sed -i "" "s/YYYY-MM-DD/$DATE/g" "$FILE"
sed -i "" "s/learning_id: /learning_id: $DATE-$TOPIC/g" "$FILE"

echo "✅ Created: $FILE"
open "$FILE"
```

**使用：**
```bash
chmod +x create-daily-learning.sh
./create-daily-learning.sh ppc-optimization-insight
```

---

### 2. 批量更新文件头部信息

```bash
# 批量更新所有 Skills 的 updated 字段
find skills/ -name "*.md" -not -path "*/_templates/*" -exec \
  sed -i "" "s/updated: .*/updated: $(date +%Y-%m-%d)/" {} \;
```

---

### 3. 生成统计报告

**创建 `generate-stats.sh`：**
```bash
#!/bin/bash

echo "=== Multi-Agent 系统统计 ==="
echo ""
echo "📝 Learnings: $(find knowledge/learnings/ -name "*.md" | wc -l)"
echo "⭐ Best Practices: $(find knowledge/best-practices/ -name "*.md" -not -name "README.md" | wc -l)"
echo "🤔 Reflections: $(find knowledge/reflections/ -name "*.md" | wc -l)"
echo "📚 Skills: $(find skills/ -name "*.md" -not -path "*/_templates/*" -not -name "README.md" | wc -l)"
echo "🤖 Agents: $(ls -d agents/*/ | grep -v "_templates" | wc -l)"
echo ""
echo "📊 本月新增:"
echo "  Learnings: $(find knowledge/learnings/$(date +%Y-%m)/ -name "*.md" 2>/dev/null | wc -l)"
echo ""
echo "🔄 Git 状态:"
git status -s | head -10
```

---

## 📞 获取帮助

### 查看文档

```bash
# 查看主 README
cat README.md

# 查看架构文档
cat ARCHITECTURE.md

# 查看快速开始指南
cat 知识库快速开始指南.md

# 查看此操作手册
cat 操作手册.md
```

### 在线资源

- Claude Code 文档: https://docs.claude.com/claude-code
- Git 教程: https://git-scm.com/doc
- Markdown 语法: https://www.markdownguide.org/

---

## 🎯 下次使用时

**记住这个命令：**
```bash
# 打开操作手册
cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent" && cat 操作手册.md
```

或者设置别名：
```bash
alias ma-help='cd "/Users/charliephil/Desktop/广告监测 agent/运营 multi agent" && cat 操作手册.md | less'
```

---

**🎊 有了这份操作手册，你可以高效地管理和使用 Multi-Agent 系统了！**

*将这份手册收藏起来，随时参考。* 📚
